/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // Génère une application Single-Page Application (SPA).
  distDir: './dist', // Change le répertoire de sortie vers `./dist/`.
}

export default nextConfig
