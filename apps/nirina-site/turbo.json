{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", "**/*.mdx", "**/*.md", "!netlify.toml"]
    },
    "test:e2e": {
      "env": ["PLAYWRIGHT_URL"]
    }
  }
}
