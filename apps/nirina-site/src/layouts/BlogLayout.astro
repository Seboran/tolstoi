---
import Prose from './../components/Prose.astro'
import Layout from './Layout.astro'

interface Props {
  frontmatter: any
  ogImage: string
}

const { frontmatter, ogImage } = Astro.props
---

<Layout title={frontmatter.title} image={ogImage}>
  <Prose><slot /></Prose>
</Layout>