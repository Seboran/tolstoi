---
interface Props {
  date: Date
}

const { date } = Astro.props

const time = date.toLocaleDateString('fr-FR', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
})
---

<dl>
  <dt class="sr-only">Publié le</dt>
  <dd class="text-base leading-6 font-medium text-dark dark:text-gray-300">
    <time datetime={date.toISOString()}>{time}</time>
  </dd>
</dl>
